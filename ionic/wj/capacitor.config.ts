import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.winejargon.tba',
  appName: 'wine jargon',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
